# 🤖 Discord Selfbot - اختبار قراءة الرسائل

## ⚠️ تحذير مهم جداً

**استخدام Selfbot يخالف شروط الخدمة من Discord ويمكن أن يؤدي إلى:**
- ❌ حظر دائم للحساب
- ❌ فقدان جميع البيانات
- ❌ عدم القدرة على إنشاء حساب جديد

**استخدم على مسؤوليتك الخاصة!**

---

## 📋 ما هو هذا البرنامج؟

Selfbot بسيط لاختبار قراءة الرسائل من Discord، مصمم خصيصاً لـ:
- ✅ قراءة الرسائل من القنوات
- ✅ فهم من أرسل الرسالة
- ✅ تحديد الردود (replies)
- ✅ تنظيف النص (بدون إيموجي وصور)
- ✅ طباعة البيانات في الترمينال

---

## 🔧 كيفية الحصول على User Token

### الطريقة 1: عبر Discord Web (الأسهل)

1. افتح Discord في المتصفح: https://discord.com/app
2. اضغط `F12` لفتح Developer Tools
3. اذهب لـ **Console** (الكونسول)
4. الصق هذا الكود:

```javascript
(webpackChunkdiscord_app.push([[''],{},e=>{m=[];for(let c in e.c)m.push(e.c[c])}]),m).find(m=>m?.exports?.default?.getToken!==void 0).exports.default.getToken()
```

5. سيظهر Token الخاص بك
6. انسخه (بدون علامات الاقتباس)

### الطريقة 2: عبر Network Tab

1. افتح Discord Web
2. اضغط `F12`
3. اذهب لـ **Network**
4. اضغط `Ctrl+R` لإعادة تحميل الصفحة
5. ابحث عن أي طلب
6. في **Request Headers**، ابحث عن `authorization`
7. انسخ القيمة

---

## 📦 التثبيت والتشغيل

### 1️⃣ تهيئة ملف `.env`

انسخ `.env.example` إلى `.env`:

```bash
cp .env.example .env
```

عدّل الملف وضع الـ token:

```env
USER_TOKEN=your_actual_token_here
```

### 2️⃣ تشغيل الـ Selfbot

```bash
cargo run --bin selfbot
```

أو للنسخة المحسّنة:

```bash
cargo build --release --bin selfbot
./target/release/selfbot
```

---

## 📊 مثال على الناتج

عند تشغيل الـ selfbot، ستظهر الرسائل بهذا الشكل:

```
============================================================
📨 رسالة جديدة
============================================================
👤 المرسل: Ahmed (ID: 123456789)
📍 القناة: 987654321 (ID: 987654321)
🏠 السيرفر ID: 111222333
↩️  رد على: Sara (ID: 555666777)
   💬 الرسالة الأصلية: "كيف حالك اليوم؟"
🕐 الوقت: 2026-07-05 18:30:00
💬 المحتوى:
   "الحمدلله تمام، شكراً على السؤال"
============================================================
```

---

## 🎯 الميزات

### ما يتم قراءته:
- ✅ اسم المرسل و ID
- ✅ القناة والسيرفر
- ✅ الوقت والتاريخ
- ✅ نص الرسالة (منظف)
- ✅ معلومات الرد (reply)
- ✅ المرفقات (أسماء فقط)
- ✅ التفاعلات (reactions)

### ما يتم تجاهله:
- ❌ الإيموجي
- ❌ الصور (لكن يظهر اسمها)
- ❌ الرموز الخاصة
- ❌ رسائل البوتات

---

## 🔍 الكود

### الملف الرئيسي: `src/selfbot.rs`

#### معالج الرسائل:
```rust
async fn message(&self, _ctx: Context, msg: Message) {
    // تنظيف وطباعة الرسالة
}
```

#### دالة التنظيف:
```rust
fn clean_text(text: &str) -> String {
    // تزيل الإيموجي وتحتفظ بالعربي والإنجليزي فقط
}
```

---

## 🛑 حل المشاكل

### الخطأ: "Invalid token"
**السبب**: Token غير صحيح  
**الحل**: أعد الحصول على token من Discord Web

### الخطأ: "Connection failed"
**السبب**: حساب محظور أو مشكلة في الشبكة  
**الحل**: 
- تحقق من حالة حسابك
- جرب VPN
- تأكد من الاتصال بالإنترنت

### الخطأ: "Permission denied"
**السبب**: الحساب ليس لديه صلاحية قراءة الرسائل  
**الحل**: تأكد من أنك عضو في السيرفر/القناة

---

## 📝 ملاحظات مهمة

1. **لا تشارك الـ Token مع أحد** - إذا تسرب، احذف الحساب فوراً
2. **Discord قد يكتشف الـ Selfbot** - احتمال الحظر مرتفع
3. **لا تستخدم على الحساب الرئيسي** - استخدم حساب تجريبي
4. **هذا للتطوير والتعلم فقط** - ليس للاستخدام الإنتاجي

---

## 🔄 الخطوات التالية

بعد التأكد من أن قراءة الرسائل تعمل بشكل صحيح، يمكن:

1. ✅ إضافة نظام حفظ البيانات
2. ✅ بناء نظام التنظيف التلقائي
3. ✅ إعداد التدريب التدريجي
4. ✅ دمج مع نموذج الذكاء الاصطناعي

---

## ⚖️ القانونية والأخلاقيات

**هذا الكود:**
- ❌ يخالف Discord ToS
- ⚠️ للأغراض التعليمية فقط
- 🔒 يجب عدم استخدامه على حسابات حقيقية
- 📚 مصمم لفهم كيفية عمل Discord API

**البدائل القانونية:**
1. استخدم Discord Bot API (القانوني)
2. استخدم Data Export من Discord
3. استخدم Webhooks للبيانات

---

## 📞 الدعم

**في حالة المشاكل التقنية:**
1. تحقق من `.env`
2. راجع logs في الترمينال
3. تأكد من إصدار Rust محدث

**إخلاء المسؤولية:**
نحن غير مسؤولين عن أي حظر أو مشاكل تنتج عن استخدام هذا الكود.

---

**آخر تحديث**: 2026-07-05  
**الحالة**: 🧪 تجريبي - للاختبار فقط
